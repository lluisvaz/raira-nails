"use client"

import * as TooltipPrimitive from "@radix-ui/react-tooltip"

const TooltipProvider = TooltipPrimitive.Provider

// Componentes Tooltip, TooltipTrigger e TooltipContent removidos - nunca foram utilizados
// Se necessário no futuro, podem ser re-adicionados do @radix-ui/react-tooltip

export { TooltipProvider }
